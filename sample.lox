class Circle {
  class PI {
    return 3.14153;
  }
  
  init(r) {
    this.radius = r;
  }
  area {
    return Circle.PI * this.radius * this.radius;
  }

  class printArea(circle) {
    print circle.area;
  }
}

var circle = Circle(10);
Circle.printArea(circle);